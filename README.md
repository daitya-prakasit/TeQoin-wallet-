# TeQoin-wallet-
100 % saf


# ============ WALLET ============
PRIVATE_KEY=0x34ca674cd22433f53b

# ============ BRIDGE MODE ============
BRIDGE_MODE=wrap
BRIDGE_ASSETS=ETH,USDT,USDC,DAI

# ============ BRIDGE AMOUNTS ============
BRIDGE_AMOUNT_ETH=0.0000001
BRIDGE_AMOUNT_USDT=0.00001
BRIDGE_AMOUNT_USDC=0.00001
BRIDGE_AMOUNT_DAI=0.00001

# ============ LOOP SETTINGS ============
LOOP_MODE=true
LOOP_INTERVAL_MIN=300
LOOP_INTERVAL_MAX=600
LOOP_COUNT=0

# ============ ANTI-BAN DELAYS (milliseconds) ============
MIN_DELAY_MS=5000
MAX_DELAY_MS=30000

# ============ TOR PROXY (Termux) ============
TOR_PROXY_HOST=127.0.0.1
TOR_PROXY_PORT=9050

# ============ RPC ============
RPC_URL=https://rpc.teqoin.io
